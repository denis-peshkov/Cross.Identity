// IntelliJ IDEA / Cursor / WebStorm — HTTP Client (.http files in this folder).
//
// Usage:
//   1. Copy http-client.private.env.json.example → http-client.private.env.json (secrets / session values).
//   2. Open a .http file (Sample.Api.http).
//   3. In the gutter, pick environment **sample** or **dev** (not "No Environment").
//   4. Run — {{HOST}}, etc. resolve from these JSON files.
//
// Public values only here. Secrets — http-client.private.env.json (gitignored).

{
  "dev": {
    "HOST": "http://localhost:5164",
    "EMAIL": "sample@example.com",
    "PASSWORD": "P@ssw0rd!",
    "TTL": "00:05:00"
  },
}
