# Глобальные правила проекта

## Общие принципы

- **Всегда отвечать на русском языке** - все комментарии, документация и общение должны быть на русском языке.
- **Строгая типизация** - использовать строгую типизацию везде, где это возможно. Избегать `any`, использовать `unknown` при необходимости.
- **Явные модификаторы доступа** - всегда указывать модификаторы доступа (`private`, `protected`, `public`). Не использовать неявные модификаторы.
- **Readonly для инжектированных сервисов** - все зависимости в конструкторе должны быть помечены как `readonly`.
- **Комментарии на русском** - все комментарии в коде должны быть на русском языке и объяснять "почему", а не "что".

## Форматирование

- **Отступ**: 2 пробела для всех файлов
- **Кавычки**: одинарные для TypeScript/JavaScript
- **Конец строки**: всегда добавлять пустую строку в конце файла
- **Пробелы**: удалять trailing whitespace

## Структура файлов

- Один класс/интерфейс/тип на файл
- Имя файла должно соответствовать имени экспортируемого класса/интерфейса (kebab-case)
- Использовать `index.ts` для реэкспорта из директорий
