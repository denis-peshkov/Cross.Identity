Ниже — **проблемы внутри библиотеки** (код `Cross.Identity/`), по уровню критичности. Аудит по дельте ветки относительно базовой ветки (обычно `master`).

**Версия:** `{{VERSION}}`
**Ветка:** `{{BRANCH}}` · **база:** `{{BASE}}` · **дата:** `{{DATE}}`
**Релиз (если есть):** {{RELEASE_URL_OR_DASH}}

**Легенда:** ⬜ open · ✅ done · 🟨 partial / принято · ❌ blocker  
**Критично / Высокий / Средний / Низкий** — только ⬜ open. **Закрыто** — все ✅; столбец `#` = `✅ #34 Session IP binding config` (номера сохраняются).

**Источники дельты:** `collect-release-delta.sh` · {{DELTA_CACHE_PATH}}

> При перепроверке: закрытый пункт **убирается** из секции критичности и переносится в `## Закрыто (проверено в коде)`:
> `| ✅ #34 Session IP binding config | …детали… |`

{{OPTIONAL_CR_OR_NOTES}}

---

## Критично (безопасность)

{{CRITICAL_OR_EMPTY}}

---

## Высокий (логика / auth model)

{{HIGH_OR_EMPTY}}

---

## Средний (противоречия / баги контрактов)

{{MEDIUM_OR_EMPTY}}

---

## Низкий (техдолг / несогласованности)

{{LOW_OR_EMPTY}}

---

## Принято (осознанный trade-off / контракт хоста)

{{ACCEPTED_OR_EMPTY}}

---

## Закрыто (проверено в коде)

| # | Суть |
|---|------|
{{CLOSED_TABLE_ROWS}}

---

## Что в библиотеке уже нормально

{{ALREADY_OK_BULLETS}}

---

## Приоритет фиксов

{{PRIORITY_OR_EMPTY}}
