# Branch triage — `release/fix-missed-issues` vs `origin/master` — 2026-08-26

**Mode:** local (Phase 1b)  
**BASE_REF:** `origin/master` (fetched)  
**BRANCH_REF:** `HEAD` (`release/fix-missed-issues`)  
**Matching PR:** [#20](https://github.com/denis-peshkov/Cross.Identity/pull/20) — MERGEABLE · CI **unstable** (CodeRabbit PENDING; `.NET` / Sonar / CodeQL SUCCESS)  
**CI / reviews in branch mode:** N/A for git delta; PR status informational only  
**Working tree:** ahead of `origin/release/fix-missed-issues` by **1** commit (triage skill docs); staged `triage-2026-08-26.md`

---

## Summary

Cross.Identity **2.3.0 BREAKING** delta: token-lifecycle bags move off compact refresh strings — **Logout / RefreshToken → `Jti`**, **LogoutAll / ChangePassword → `UserAccountId`**; slim `IJwtTokenService`; host authorizes before `ExecuteAsync`. Same branch carries release-plan / triage / CodeRabbit tooling. CodeRabbit findings from 2026-08-26 closed in `docs/RELEASE-PLAN-2.3.0.md` (open C/H/M/L empty).

**Size:** XL — 20 commits · 61 files · +1347 / −942

---

## Commits (`origin/master..HEAD`)

| Focus | Commits (representative) |
|-------|--------------------------|
| Flows JWT | Logout / LogoutAll / RefreshToken → Jti / UserAccountId; JWT API slim |
| Docs | `BREAKING.md` §2.3.0, `FLOWS.md`, `README`, `RELEASE-PLAN-2.3.0.md` |
| CR follow-ups | H11–H13, M52–M58 closed |
| Tooling | release-plan scripts, triage-pr Phase 1b, orchestrator **local/branch** modes |
| Misc | ExternalLogin field order; skill renames |

---

## Diff stat / hotspots

### Library (auth)

| Path | Change |
|------|--------|
| `Services/IJwtTokenService.cs` / `JwtTokenService.cs` | Remove unused string refresh helpers; Guid/Jti rotation & logout paths |
| `Steps/*Logout*`, `RefreshTokenStep` + factories | Bag keys → `Jti` / `UserAccountId` |
| `main.Logout.json`, `main.RefreshToken.json`, `main.LogoutAll.json`, `main.ChangePassword.json` | collectForm contract |

### Tests

| Path | Change |
|------|--------|
| `TokenTestHelpers.cs` | Test-only active-refresh checks (+ `CancellationToken`) |
| `JwtTokenServiceTests`, `Main_*Logout*`, `Main_RefreshToken_*` | Updated to new contracts |

### Docs / tooling

| Path | Change |
|------|--------|
| `docs/BREAKING.md`, `FLOWS.md`, `README.md` | Host guidance for 2.3.0 |
| `.cursor/skills/{triage,triage-pr,release-plan,coderabbit}/` | Workflows; local triage |

---

## Risks (auth / JWT)

| Risk | Notes |
|------|--------|
| **Host misuse** | Library trusts bag `Jti` / `UserAccountId` — host **must** validate JWT / authorize caller before `ExecuteAsync` |
| **BREAKING consumers** | Old bags with compact `RefreshToken` fail validation; removed public JWT helpers |
| **Session binding / idle** | Unchanged model on refresh path; still depends on host-supplied client context when configured |

No new open plan security items; CR did not flag JwtTokenService/steps on last review after fixes.

---

## Priority actions

1. Push the **+1** local commit (skill docs) if it should be on PR #20; else leave out of PR.
2. Wait for **CodeRabbit PENDING** → green on [#20](https://github.com/denis-peshkov/Cross.Identity/pull/20).
3. Merge to `master` → tag / NuGet **2.3.0**.
4. Optional: `triage local deep` for defect-first pass beyond this summary.

---

## Numeric summary

| Metric | Value |
|--------|-------|
| Commits | 20 |
| Files | 61 |
| Lines | +1347 / −942 |
| Size band | XL |
| Open RELEASE-PLAN C/H/M/L | 0 |
| Matching open PR | 1 (#20) |

---

_Generated by skill `triage` · mode **local** · base `origin/master`._
